name: Deploy Simples

on:
  push:
    branches:
      - main   

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout do código
      uses: actions/checkout@v3

    - name: Build simples
      run: |
        echo "Deploy bem-sucedido!"
